# SimCity (USA) ROM identity
# This file is the single source of truth for ROM digests.
# The build, tools/regen.sh, release workflow, and launcher all read this.
#
# Format: key = value (one per line, no inline comments)
# Values are bare hex (no 0x prefix).

# Expected CRC32 of the verified SimCity (USA) ROM
expected_crc32 = 8aedd3a1

# Expected SHA-256 of the verified SimCity (USA) ROM
expected_sha256 = e9c0bc05511e05a0d7c3e7cc42e761e1e8e532d46f59b9854b6902e1a2e9dd0a

# Human-readable title (used in launcher, window title)
title = SimCity (USA)

# Short slug (used in save directory, config file names)
slug = simcity

# ROM file name (for regen.sh default lookup)
rom_file = SimCity (USA).sfc

# Mapper type: lorom or hirom
mapper = lorom

# Cartridge type: rom, rom_ram, rom_ram_sram, etc.
cart_type = rom_ram_sram

# SRAM size in KB (0 if none)
sram_kb = 32

# Region: ntsc, pal, or auto
region = ntsc

# Version byte from ROM header
version = 0